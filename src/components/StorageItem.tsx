import React from "react";

export const StorageItem = (props: any) => { 
    return (<tr>
            {props.children}
        </tr>)
    //TODO: create a component that will be used in the storage page for displaying the items
 }